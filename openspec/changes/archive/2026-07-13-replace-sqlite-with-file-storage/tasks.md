## 1. Adopt the file-storage contract

- [x] 1.1 Define complete `STO-001` through `STO-007` deltas for durable data paths, transaction journals, snapshots, migrations, recovery, permissions, redaction, and bounded maintenance.
- [x] 1.2 Define the `OBS-011` JSON Lines segment, cursor, rotation, crash-tail, corruption, and derived-index contract.
- [x] 1.3 Record the accepted FileStore layout, single-writer boundary, atomic publication algorithm, migration policy, rejected alternatives, and native-platform verification seams.
- [x] 1.4 Pass strict OpenSpec and repository-specific validation before archiving the contract into the target 1.0 specs.
