## 1. Contract and design alignment

- [x] 1.1 Define the complete `IPC-001` delta for the Windows transport-authentication preface, SID boundary, verified impersonation revert, and fail-closed denial behavior.
- [x] 1.2 Align the active `secure-local-control-plane` proposal and design with authentication before protocol-frame buffering rather than before the first transport byte.

## 2. Contract verification

- [x] 2.1 Validate the complete modified requirement and every scenario with strict OpenSpec validation.
- [x] 2.2 Confirm that the active implementation change retains separate tasks for platform ACL evidence, bounded first-frame timing, authenticated readiness, and protocol framing.
