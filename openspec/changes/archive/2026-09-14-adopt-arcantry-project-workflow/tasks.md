## 1. Shared project workflow

- [x] 1.1 Add shared Arcantry configuration for OpenSpec, root todo, changelog and local release planning.
- [x] 1.2 Add root `todo.txt` and shared agent guidance without changing Cadder tooling or CI.

## 2. Release story

- [x] 2.1 Record the `0.8.0` baseline and internal release meaning for all archived changes.
- [x] 2.2 Add preliminary release meaning to active changes and render the initial public changelog boundary.

## 3. Private agent operation

- [x] 3.1 Add ignored private guidance and a local Arcantry launcher without a shadowing private configuration.
- [x] 3.2 Link the missing promotion skill and verify the user-scope skill catalog.

## 4. Verification

- [x] 4.1 Verify one active shared configuration, independent shared/private todo behavior and the expected release plan.
- [x] 4.2 Run Arcantry repository checks and strict OpenSpec validation while preserving the existing dirty worktree.
